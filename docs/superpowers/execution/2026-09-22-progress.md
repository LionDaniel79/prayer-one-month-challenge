# SDD ledger — plan: docs/superpowers/plans/2026-09-22-prayer-one-month-challenge.md

Execution mode: Native fallback because this harness exposes no general-purpose subagent dispatch tool.

Pre-flight: Task 1 produces env/test/build foundation consumed by Tasks 2-9; no interface conflict found.
Pre-flight: Task 2 DB client/schema are consumed by Tasks 4-7; no interface conflict found.
Pre-flight: Task 3 date/progress functions are consumed by Tasks 6-7; no interface conflict found.
Pre-flight: Task 4 session/auth interfaces are consumed by Tasks 5-7; no interface conflict found.
Pre-flight: Task 5 sam/profile interfaces are consumed by Tasks 6-7; no interface conflict found.
Pre-flight: Task 6 member dashboard is independent from Task 7 admin view over shared domain services; no conflict found.

Task 1: RED observed in GitHub Actions run 35669444702 — src/lib/env.test.ts failed because ./env did not exist.
Task 1: Ruling: initial CI disabled setup-node npm caching until package-lock exists — setup-node cache requires a dependency lockfile, while Task 1 intentionally starts before lock generation — cost if wrong: slower installs during initial commits.
Task 1: Ruling: use npm alias typescript=npm:@typescript/typescript6@6.0.2 plus @typescript/native=npm:typescript@7.0.2 — typescript-eslint/Next tooling imports the TS6 programmatic API while TS7 remains available during the transition — cost if wrong: tooling/compiler behavior may diverge and require a later version adjustment.
Task 1: Ruling: pin ESLint 9.39.5 instead of ESLint 10.11.0 — eslint-config-next currently pulls eslint-plugin-react that crashes on ESLint 10's removed context.getFilename API — cost if wrong: temporary use of an EOL linter major until upstream support lands.
Task 1: Ruling: expose getEnv() as a lazy validated accessor rather than parsing process.env at module import — CI/build and parser unit tests need to import the module before deployment secrets exist; first server-side use still validates all required values — cost if wrong: a missing runtime secret is detected on first server use rather than at build/import time.
Task 1: complete (commits 27ec029..6f2d43e, verification: GitHub Actions run 35670014470 — npm ci, npm test, npm run lint, npm run build all PASS).
Task 2: RED observed in GitHub Actions run 35670917408 — tests/db/schema.test.ts failed because src/db/schema was absent.
Task 2: Ruling: keep RLS enabled on the private prayer_app schema and add explicit restrictive deny-all policies for non-bypass roles — removes Advisor warnings while preserving backend-only access — cost if wrong: a future switch to direct client/Data API access will require deliberate policy redesign.
Task 2: Ruling: DB smoke behavior verified through Supabase execute_sql transaction/rollback because the connected management API does not expose the project database password/transaction-pooler secret needed to run scripts/smoke-db.ts from CI — cost if wrong: the Node smoke wrapper itself remains to be exercised once deployment credentials are available.
Task 2: complete (commits a5fb2a5..f590458, verification: GitHub Actions run 35671232631 PASS; Supabase migration applied; 6 tables + indexes verified; Security Advisor 0 findings; smoke linked_rows=1 then rollback left 0 rows).
Task 3: RED observed in GitHub Actions run 35671368924 — date/progress modules absent.
Task 3: complete (commits 7b019fb..92742e9, verification: GitHub Actions run 35671488014 — challenge date/progress tests, lint, build PASS).
Task 4: RED observed in GitHub Actions run 35671635583 — auth modules absent.
Task 4: Ruling: exact name+phone lookup returns REGISTRATION_REQUIRED when no exact account exists; it never reveals whether the same name exists with a different phone — preserves requested first-use signup while avoiding cross-account disclosure — cost if wrong: a mistyped phone can lead the user toward a new registration rather than a password error.
Task 4: complete (commits 76e7e8b..557c13a, verification: GitHub Actions run 35671880946 — 26 tests, lint, build PASS; Argon2id, 180-day HttpOnly session, same-name/different-phone, and 8-attempt throttle tests PASS).
Task 5: RED observed in GitHub Actions run 35671998262 — sams/service module absent.
Task 5: complete (commits dea3cc5..412d91b, verification: GitHub Actions run 35672284622 — sam search tests, auth suite, lint, build PASS; login registration UI, sam/leader search, profile sam change, session heartbeat added).
