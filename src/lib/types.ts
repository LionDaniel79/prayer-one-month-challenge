export type SamOption = {
  id: string;
  name: string;
  leaderName: string;
};

export type ProfileData = {
  displayName: string;
  samId: string | null;
  samName: string | null;
  samLeaderName: string | null;
};
